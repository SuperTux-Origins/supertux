/* Placeholder so ndk-build can validate the Android.mk skeleton.
 * Replace with the full SuperTux source list when wiring the APK. */
#include <SDL.h>
extern "C" int SDL_main(int, char**) {
  return 0;
}
